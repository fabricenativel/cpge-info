{{titre_concours("CCMP","2025")}}


!!! note
    * Ce corrigé peut contenir des erreurs ! Merci de me les signaler si vous en trouvez
    
## Enoncé

{{telecharger("Enoncé","CCMP2025.pdf")}}

## 1 - Base de données - Exemple de fret maritime de conteneurs

### Q1
```sql
SELECT idC, val*1.0/taille as ratio
FROM CONTENEURS
WHERE portDepC = 'Marseille' AND portDestC = 'Barcelone' AND dateDisp < '2025-01-01'
ORDER BY ratio DESC;
```

### Q2

!!! warning3 "Attention"
    La requête suivante ne liste *pas* les navires n'ayant aucun conteneur, pour les inclure il faudrait une jointure gauche (`#!sql LEFT JOIN`) qui est hors programme.

```sql
SELECT NAVIRES.idN, COUNT(CONTENEURS.idC) AS nbConteneurs
FROM NAVIRES
JOIN CONTENEURS ON CONTENEURS.idN = NAVIRES.idN
GROUP BY NAVIRES.idN;
```

## 2 - Structure de données pour l'espace des objets

### Q3
```python linenums='1'
--8<-- "CCMP2025/CCMP2025.py:Q3"
```

### Q4
```python linenums='1'
--8<-- "CCMP2025/CCMP2025.py:Q4"
```

!!! note
    Le rapport de jury pointe l'utilisation d'instructions conditionnelles telles que :
    ```python
    if poids<=b:
        return True
    else:
        return False
    ```
    à la place du `#!python return  poids <= b` (ligne 7), comme un manque de "*familiarisation*" avec les booléens. On rappelle donc, que l'expression `#!c poids<=b` est un expression booléenne valant `True` ou `False`. On peut donc renvoyer directement sa valeur, au lieu de ... renvoyer `True` si elle vaut `True` et de renvoyer `False` sinon. 

## 3 - Structure de données pour l'espace des solutions

### Q5
* Pour la feuille `b`, la liste `S` vaut `[1, 1, 0]`
* Pour la feuille `c`, la liste `S` vaut `[1, 0, 1]`

### Q6
Pour $n$ objets, l'arbre binaire possède $2^n$ feuilles (à chaque niveau de l'arbre le nombre de feuilles double). On doit exécuter les fonctions `profit` et `contrainte` sur chaque choix possible. Ces deux fonctions sont de complexité linéaire, la complexité de la force brute serait donc en $\mathcal{O}(n2^n)$.

## 4 - Résolution approchée par un algorithme glouton

### Q7
Avec la liste `obj` donné en exemple la liste des ratios `Lqi` une fois triée est `[4, 1.5, 1]`, la liste des `Li` correspondante est `[1, 0, 2]`. L'objet 1 est donc sélectionné (poids restant = 4), puis l'objet 2 (poids restant = 2) et on ne peut pas prendre l'objet 3. Donc la liste `S` construite sera `[1, 1, 0]`.

### Q8
```python linenums='1' hl_lines="5 12 15"
--8<-- "CCMP2025/CCMP2025.py:Q8"
```

### Q9

!!! note
    La fonction `construitLi` est une implémentation du *tri par insertion*, déjà rencontré en CPGE.

* *Meilleur cas :* Il se produit lorsque la condition de la boucle `while` (`Lqi[j-1] < x`) est immédiatement fausse à chaque itération. Cela signifie que la liste est initialement *déjà triée dans l'ordre décroissant*. Dans ce cas, le corps de la boucle `while` n'est jamais exécuté. La complexité est alors *linéaire* en $\mathcal{O}(n)$, car la boucle `for` principale effectue simplement $n-1$ itérations à coût constant.
* *Pire cas :* Il se produit lorsque la condition de la boucle `while` est vraie jusqu'à ce que $j=0$. Cela signifie que chaque nouvel élément doit être inséré tout au début, c'est-à-dire lorsque la liste est initialement *triée dans l'ordre croissant*. La boucle `while` effectue alors $i$ itérations pour décaler les éléments. Le nombre total de décalages est proportionnel à $\sum_{i=1}^{n-1} i = \frac{n(n-1)}{2}$. La complexité est alors *quadratique* en $\mathcal{O}(n^2)$.

### Q10
```python linenums='1' hl_lines="4 5 6 7"
--8<-- "CCMP2025/CCMP2025.py:Q10"
```

### Q11
L'algorithme glouton va sélectionner les objets par ratio décroissant : d'abord l'objet d'indice 1 (ratio 4), puis l'objet d'indice 0 (ratio 1.5). Le poids total est alors de $1+2=3 \leqslant 5$, et le profit obtenu est de $4+3=7$. Il ne peut plus ajouter l'objet d'indice 2.

Cependant, en sélectionnant les objets d'indices 1 et 2, le poids total est de $1+4=5 \leqslant 5$ et on obtient un profit de $4+4=8$. On vérifie aisément que c'est le choix optimal. La solution fournie par l'algorithme glouton (profit 7) n'est donc pas optimale sur cet exemple, ce qui illustre le fait qu'une stratégie gloutonne ne garantit pas toujours la solution exacte au problème du sac à dos.

## 4 - Résolution exacte par un algorithme de programmation dynamique